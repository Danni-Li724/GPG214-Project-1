using System.IO;
using UnityEngine;

public class ClassPractice : MonoBehaviour
{
    // [SerializeField] private string folderName = "Saves";
    // [SerializeField] private string fileName = "save1.csv";
    //
    // private string saveCsvPath;
    //
    // private void Awake()
    // {
    //     string savesFolderPath = Path.Combine(Application.persistentDataPath, folderName);
    //     Directory.CreateDirectory(savesFolderPath);
    //
    //     saveCsvPath = Path.Combine(savesFolderPath, fileName);
    //     Debug.Log("ClassPractice save path: " + saveCsvPath);
    // }
}
